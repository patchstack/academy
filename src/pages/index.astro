---
const base_var = process.env.BASE;
const base = base_var ? base_var + "/" : '/';
const url = base + 'welcome/'.replace('//', '/');

const url_base = process.env.DEPLOY_PRIME_URL ? process.env.DEPLOY_PRIME_URL + '/' : Astro.site.href;
const redirect = url_base + url;
const test = process.env.BASE;
const test2 = process.env.DEPLOY_PRIME_URL;
---

{ test }
{ test2 }